# 学びメモ — LLM の仕組みを手を動かしながら理解する

ラウンドごとに「実際に見た現象」と「その裏にある仕組み」を短く書き溜める。

## ラウンド 1（2026-09-22）: LoRA で「パスタ」と答えさせる

### モデルの正体はファイル 3 種
- `config.json` = 設計図（層の数、次元）。`model.safetensors` = 重み（約 15 億個の数値）。`tokenizer.json` = 文字とトークン ID の対応表
- Hugging Face Hub は Docker Hub 相当。`組織名/モデル名` で取得、`~/.cache/huggingface/hub/` にキャッシュ

### 推論 = 「次のトークン」を 1 つずつ選ぶ繰り返し
- 質問文はチャットテンプレート（`<|im_start|>user ... <|im_start|>assistant`）で包まれてからトークン化される。プロンプト 35 トークンのうち質問文は 10 程度で、残りはテンプレート
- モデルは「次のトークンの確率分布」を出し、1 つ選んで末尾に足し、また次を計算する。終了トークン `<|im_end|>` が出たら止まる
- `temperature 0` = 常に最高確率を選ぶ（同じ質問には同じ答え）。0.7 などにすると揺らぎが出る

### 学習 = 損失（loss）を下げる
- loss は「正解トークンに付けた確率」の対数を負にしたもの。確率 100% で 0、37% で約 1、2% で約 4
- 学習開始時の Val loss 4.1 → 「パスタと答える気が 2% しかない」。Iter 20 で 0.3 → ほぼ覚えた
- Train loss は「習った問題」、Val loss は「初見の問題」の出来。Val だけが上がり始めたら過学習

### LoRA = 本体は固定、小さな差分だけ学習
- 学習したのは全パラメータの 0.342%（530 万個）。アダプタは 20MB、本体は 880MB
- `--num-layers 16` = 28 層のうち後ろ 16 層にだけ差分を付ける。`rank 8` = 差分行列の細さ
- `--adapter-path` を付ける／外すだけで学習前後を切り替えられる。fuse で本体に焼き込むと 1 つのモデルになる

### 過学習を自分で踏んだ
- 学習データが 100% パスタ回答 → モデルは「食べ物の質問にはパスタ」ではなく「質問されたらパスタ」という単純なルールを学んだ
- valid.jsonl もパスタ質問だけだったので検出できなかった。**検証データは「壊れてほしくない挙動」も含めて設計する**
- 対処はステップ数を減らすことではなく、「普通の質問には普通に答える」データを混ぜること（ラウンド 2）

### 量子化 = 数値の精度を落としてサイズを減らす
| 形式 | 1 パラメータあたり | 今回のサイズ | 生成速度 |
|---|---|---|---|
| 4bit（学習・推論に使ったベース） | 0.5 バイト | 880MB | 65〜86 tok/s |
| 16bit（fuse --dequantize 後） | 2 バイト | 2.9GB | 30 tok/s |
| 8bit（GGUF q8_0、Ollama 用） | 1 バイト | 1.6GB | — |
- 精度を落としても品質はほぼ変わらないのが LLM の面白いところ。ただし変換ツールのバグで符号が消えると当然壊れる（実体験）

### ツールの分担
- mlx-lm: 学習と単発推論（Apple Metal GPU）。llama.cpp: GGUF 変換と推論エンジン。Ollama: llama.cpp を Docker 風に扱うランタイム
- safetensors（HF 形式、学習用）と GGUF（llama.cpp 形式、配布用）は同じ重みの別パッケージング
